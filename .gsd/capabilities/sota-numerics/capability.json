{
  "id": "sota-numerics",
  "role": "feature",
  "version": "0.2.0",
  "title": "SOTA/efficiency/numerical-stability steering",
  "description": "Advisory steering across gsd's plan/execute/verify/ship lifecycle toward SOTA numerics: internally consistent and project-consistent, unambiguous, complete, efficient, quiet-running, agent-facing code -- plus the single blocking plan:post gate that mechanically enforces a compliant Alternatives Considered section on every plan in a phase.",
  "tier": "full",
  "requires": [],
  "engines": {
    "gsd": ">=1.10.0"
  },
  "runtimeCompat": {
    "supported": [
      "*"
    ],
    "unsupported": []
  },
  "skills": [],
  "agents": [],
  "hooks": [],
  "config": {
    "sota-numerics.enabled": {
      "type": "boolean",
      "default": true,
      "description": "Master toggle for both the advisory steering fragments and the blocking plan:post Alternatives Considered gate \u2014 the only config key this capability declares."
    }
  },
  "steps": [],
  "contributions": [
    {
      "point": "plan:pre",
      "into": "planner",
      "produces": [],
      "consumes": [],
      "fragment": {
        "path": "fragments/planner-sota.md"
      },
      "when": "sota-numerics.enabled",
      "onError": "skip"
    },
    {
      "point": "execute:wave:pre",
      "into": "executor",
      "produces": [],
      "consumes": [],
      "fragment": {
        "path": "fragments/executor-numerics.md"
      },
      "when": "sota-numerics.enabled",
      "onError": "skip"
    },
    {
      "point": "execute:wave:post",
      "into": "verifier",
      "produces": [],
      "consumes": [],
      "fragment": {
        "path": "fragments/verifier-precision.md"
      },
      "when": "sota-numerics.enabled",
      "onError": "skip"
    },
    {
      "point": "ship:pre",
      "into": "orchestrator",
      "produces": [],
      "consumes": [],
      "fragment": {
        "path": "fragments/ship-precision-advisory.md"
      },
      "when": "sota-numerics.enabled",
      "onError": "skip"
    }
  ],
  "gates": [
    {
      "point": "plan:post",
      "check": {
        "predicate": {
          "kind": "command-exit-zero",
          "command": "_SN=.gsd/capabilities/sota-numerics/scripts/check-alternatives.py; SOTA_SCRIPT=\"./$_SN\"; [ -f \"$SOTA_SCRIPT\" ] || SOTA_SCRIPT=\"${GSD_HOME:-$HOME}/$_SN\"; test -f \"$SOTA_SCRIPT\" || { echo \"sota-numerics: gate script not found at project or global scope -- run 'gsd capability install sota-numerics' (or reopen the session to let the SessionStart auto-install run)\" >&2; exit 1; }; python3 \"$SOTA_SCRIPT\"",
          "timeout": 30
        }
      },
      "when": "sota-numerics.enabled",
      "blocking": true,
      "onError": "halt",
      "description": "onError is deliberately \"halt\": it routes a malformed predicate declaration or an unknown predicate kind \u2014 the only outcomes gsd-core's predicate evaluator throws on. Every runtime outcome of the check command itself is mapped to a block verdict instead: a missing python3 exits 127, a crash exits 1, and the 30s timeout all produce block, and only exit 0 does not. A blocking gate that silently skips when its own manifest is malformed defeats its purpose. Do not \"fix\" this back to \"skip\"."
    }
  ]
}
